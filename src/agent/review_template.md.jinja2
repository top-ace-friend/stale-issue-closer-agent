# Review Issue #{{ number }}: {{ title }}

URL: {{ url }}

## Proposed Actions

{% for a in actions %}
- **{{ a.name }}**: {% if a.value is sameas true %}✅ {% endif %}{% if a.value is iterable and a.value is not string %}{{ a.value | join(', ') if a.value else 'None' }}{% else %}{{ a.value }}{% endif %}{% if a.rationale %}  
	Rationale: {{ a.rationale | replace('\n', '\n  ') }}{% endif %}

{% endfor %}

### Overall Rationale

{{ overall_rationale }}

### Next steps

Then you can either:

- Accept: approve as-is.
- Edit: modify any of the fields.
- Respond: leave a note (no changes).
- Ignore: skip this issue.
